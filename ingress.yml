apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: feedback_pipeline
  annotations:
    ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - http:
      paths:
        - path: /feedback
          backend:
            serviceName: feedback_pipeline
            servicePort: 8085
